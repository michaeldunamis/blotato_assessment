DATABASE_URL="postgresql://blotato:blotato@localhost:5432/blotato_comments?schema=public"
PORT=3000

# How long a post's locally cached comments are considered fresh before
# a GET request triggers a synchronous re-pull from the platform adapter.
COMMENT_CACHE_TTL_SECONDS=300
